fastapi==0.111.0
uvicorn[standard]==0.29.0
# Pillow >= 12.2.0 required:
#   CVE: FITS GZIP decompression bomb (patched in 12.2.0)
#   CVE: out-of-bounds write loading PSD images (patched in 12.1.1)
Pillow==12.2.0
opencv-python-headless==4.9.0.80
numpy==1.26.4
# rembg is NOT included here.
# All known versions of rembg (<= 2.0.57) have an unpatched CORS
# misconfiguration vulnerability (no fix released by upstream).
# If you need AI background removal, install rembg manually in a controlled
# environment and review the security implications before deploying:
#   pip install rembg==2.0.56
# See: background_remover.py for details.
# python-multipart >= 0.0.22 required:
#   CVE: arbitrary file write via non-default configuration (patched in 0.0.22)
#   CVE: DoS via malformed multipart/form-data boundary (patched in 0.0.18)
python-multipart==0.0.22
aiofiles==23.2.1
